#include <iostream>
using namespace std;
/*struct link
{
    int value;
    link* next;
};
class linklist
{
private:
   link* first;
public:
   linklist()
   {
       first = NULL;
   }
   void additem(int d);
   void print();
   int GetSize();
   int top();
   int GetElement(int n);
   //void pop();
};
void linklist::additem(int d)
{
    link* newlink = new link;
    newlink->value = d;
    newlink->next = first;
    first = newlink;
}
void linklist::print() {
    link* current = first;
    while(current)
    {
        cout<<current->value<<endl;
        current = current->next;
    }
}
int linklist::GetSize()
{
    int size = 0;
    link* current = first;
    while(current)
    {
        size++;
        current = current->next;
    }
    return size;
}
int linklist::GetElement(int n)
{
    int cur = 0;
    link* current = first;
        while (current) {
            if(cur == n) {
                return (current->value);
            }
            current = current->next;
            cur++;
        }
        return 0;
}
int linklist::top()
{
    return (first->value);
}
void linklist::pop()
{
    link* current = first;
    delete current;
}
int main()
{
    linklist l1;
    l1.additem(11);
    l1.additem(15);
    cout<<l1.top()<<endl;
    l1.additem(25);
    l1.additem(45);
    cout<<l1.GetSize()<<endl;
    cout<<l1.GetElement(0)<<endl;
   // l1.pop();
    l1.print();*/
int main() {
   /* struct queue{
int a[1000];
int head = 0;
int tail = 0;
void push(int x)
{
    a[++tail] = x;
}
int pop()
{
    return(a[tail--]);
}
bool isEmpty()
{
    return tail==head;
}
    };
    */d
}